"use client";

import { AccountsView } from "@/components/accounts/AccountsView";

/** The Accounts dashboard: every account, every balance, recent movement. */
export default function AccountsPage() {
  return <AccountsView />;
}
