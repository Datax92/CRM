"use client";

import { CapitalInvestmentView } from "@/components/accounts/CapitalInvestmentView";

/**
 * Capital Investment — the ventures, as the `CAPITAL INVESTMENT` sheet stacks
 * them: a named pot with what was put in on one side and what it was spent on
 * on the other.
 */
export default function CapitalInvestmentsPage() {
  return <CapitalInvestmentView />;
}
