"use client";

/**
 * An employee's Facebook leads — offers waiting on them, and the ones they took.
 * Everything is in `MetaLeadsView`, including the phone layout.
 */

import { MetaLeadsView } from "@/components/leads/MetaLeadsView";

export default function EmployeeMetaLeadsPage() {
  return <MetaLeadsView />;
}
